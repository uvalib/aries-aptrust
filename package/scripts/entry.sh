# run application

# run from here, since application expects json template in templates/
cd bin; ./aries-aptrust -aptrust $ARIES_APTRUST_URL -apiuser $ARIES_APTRUST_USER -apikey $ARIES_APTRUST_KEY

#
# end of file
#
